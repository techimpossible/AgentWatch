# Swift / SPM build artifacts
.build/
.swiftpm/
Package.resolved
*.xcodeproj
*.xcworkspace

# macOS
.DS_Store

# Built bundle and DMGs (regenerated by build.sh)
AgentWatch.app/
AgentWatch*.app/
*.dmg

# Debug logs (only present when AGENTWATCH_DEBUG=1 is set)
/tmp/agentwatch.log

# Local Claude Code state (worktrees, settings, session data)
.claude/
